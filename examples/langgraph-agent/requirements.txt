# Pinned to versions known to work together for this demo. Bump if needed
# when LangGraph / LangChain release breaking changes.
langgraph>=0.2,<0.4
langchain-anthropic>=0.3,<0.4
langchain-core>=0.3,<0.4
typing-extensions>=4.9
